The RBAC Middleware opens a PostgreSQL connection scoped with parameters derived from the JWT token—tenant ID (tenant_id), user ID (user_id), and user roles (admin/auditor/operator)—extracted through OIDC/IdP authentication. This ensures data segregation is enforced according to RLS policies.