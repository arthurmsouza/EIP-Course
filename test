RBAC Application


sequenceDiagram
    autonumber
    participant U as User (Browser)
    participant P as Portal (app.sua-app.com)
    participant N as NGINX Edge (mTLS gateway)
    participant R as RBAC Middleware (api.sua-app.com)
    participant K as Keycloak (login.sua-idp.com)
    participant CA as PKI (Root/Intermediate CA)
    participant S as Session Store (Redis)
    participant A as Downstream API

    %% ---- ONE-TIME PKI ENROLLMENT ----
    rect rgba(235, 245, 255, 0.7)
    U->>CA: Request client certificate (CSR) 
    CA-->>U: Issue client cert (User.crt) signed by Intermediate CA
    note over U,CA: Cert instalado no browser (ou no dispositivo) com a chave privada
    end

    %% ---- FIRST CONTACT: mTLS @ NGINX ----
    U->>N: TLS handshake (Server presents N cert; Client presents User.crt)
    N-->>U: TLS established if cert valid (verify chain, CRL/OCSP, expiry)
    note over N: ssl_verify_client on;<br/>verify against CA bundle

    %% ---- LOGIN INIT ----
    U->>P: Open Portal (/)
    P->>N: GET /auth/login?next=/dashboard (cookie/samesite)
    N->>R: Proxy /auth/login (mTLS upstream optional)
    R->>R: Generate state (and optional nonce/PKCE)
    R-->>U: 302 → K /auth (client_id, response_type=code,<br/>redirect_uri=https://api.sua-app.com/auth/callback, scope, state[, code_challenge])

    %% ---- KEYCLOAK INTERACTIVE LOGIN ----
    U->>K: GET .../auth (Login Page)
    K->>U: Render login + MFA/WebAuthn
    U->>K: Submit credentials/MFA

    %% ---- CALLBACK TO RBAC (CONFIDENTIAL CLIENT) ----
    K-->>U: 302 → https://api.sua-app.com/auth/callback?code&state
    U->>N: GET /auth/callback?code&state (mTLS again)
    N->>R: Proxy /auth/callback

    R->>K: POST /token (authorization_code, client_id, client_secret[, code_verifier])
    K-->>R: {access_token, refresh_token, id_token, expires_in}
    R->>S: Save session {sub, roles, AT, RT, exp}
    R-->>U: Set-Cookie sid=... (HttpOnly, Secure, SameSite) + 302 → https://app.sua-app.com/dashboard

    %% ---- API USE WITH SESSION COOKIE + mTLS EDGE ----
    U->>P: GET /dashboard
    P->>N: XHR GET /api/me (credentials: include; mTLS)
    N->>R: Proxy /api/me (optionally mTLS upstream)
    R->>R: Load session; refresh AT if near expiry
    R->>A: GET /me (Authorization: Bearer <AT>)
    A-->>R: 200 { profile/claims }
    R-->>P: 200 { profile/claims }

    %% ---- LOGOUT ----
    U->>P: Click Logout
    P->>N: POST /auth/logout (mTLS)
    N->>R: Proxy /auth/logout
    R->>S: Destroy session; clear cookie
    R->>K: (optional) end_session → post_logout_redirect_uri=https://app.sua-app.com/
    K-->>U: 302 → Portal


















RBAC API

The RBAC Middleware ensures privacy and enforces authorization rules across the bridge workflow. Each Issuer integrates with the RBAC Middleware to maintain an isolated privacy domain. The RBAC Middleware is responsible for authorizing mint and burn operations, ensuring only valid and approved transactions move across the networks.

⸻

4.3.4.2 Bridge interaction flow

Bridge from Enterprise to Swift SLE
	5.	A participant in the Enterprise Network initiates a deposit request via the Backend application by first approving the TokenBridge Contract to move funds from the ERC20/ERC3643 contract and locking tokens in the TokenBridge Contract.
	6.	The TokenBridge Contract emits an event with details (amount, token, destinationAddress).
	7.	The Issuer Bridge Operator monitors and captures this event via a secure message channel.
	8.	The Issuer Bridge Operator, using a gas-sponsor wallet, pays the gas on behalf of the Enterprise participant and sends the transaction details to the RBAC Middleware.
	9.	The RBAC Middleware validates the request and authorizes the mint of the private tokens inside the Swift SLE environment.

⸻

Bridge from Swift SLE to Enterprise
	6.	A participant in the Enterprise Network initiates a withdrawal request via the Backend application, which sends a request to the Issuer Bridge Operator.
	7.	The Issuer Bridge Operator forwards the withdrawal request to the RBAC Middleware for authorization.
	8.	The RBAC Middleware validates the burn, generates a proof of burn, and returns it to the Issuer Bridge Operator.
	9.	The Issuer Bridge Operator sends the validated proof back to the Enterprise TokenBridge Contract.
	10.	The TokenBridge Contract releases the locked tokens to the specified address on the Enterprise Network.

------
Generate PKI client cert (bank → gets installed on their machine/app).
	2.	Create API key in API Gateway (send to bank).
	3.	Create Keycloak client_id/secret (send to bank).
	4.	Bank calls OIDC → get JWT → call API with mTLS + API key + JWT.


Bank ↔ API Gateway: mTLS + API Key (+ JWT you issue via RBAC broker endpoint)
	•	API Gateway ↔ RBAC: private (VPC Link)
	•	RBAC ↔ Keycloak: internal only (HTTPS), RBAC holds the client secrets

Onboarding steps (day-1 for a new bank)

1) PKI + Gateway
	1.	Issue client cert for the bank (or sign their CSR).
	2.	Add your CA to API Gateway mTLS truststore (S3 PEM).
	3.	Create API Key (bankX-prod) + Usage Plan; require on routes.
	4.	Keep Gateway JWT Authorizer enabled (we’ll feed it a Keycloak token).

2) Keycloak (internal only)
	1.	Create client: bankX-api-client (OIDC, confidential; service accounts ON).
	2.	Assign roles/scopes (bridge:deposit, etc.).
	3.	Add mappers:
	•	aud = my-enterprise-api
	•	org_id = bankX
	•	include roles in token
	4.	Copy client_secret. Do NOT share with bank.

3) RBAC DB (tenant & allowlists)
	•	Map cert subject ⇄ org_id=bankX
	•	Map API key id ⇄ org_id=bankX
	•	Seed allowlists (wallets, contracts, function_sigs).

4) RBAC exposes a /token endpoint (Token Broker)
	•	Bank does not talk to Keycloak.
	•	Bank calls RBAC /token over Gateway with mTLS + API key.
	•	RBAC authenticates bank (mTLS+API key), finds org_id, looks up bank’s Keycloak client creds, calls Keycloak token endpoint (client_credentials), returns the access_token (JWT) to the bank.
5) Bank calls your real APIs
Gateway validates:
	•	mTLS client cert ✅
	•	x-api-key usage plan ✅
	•	JWT (issuer/audience/signature) via JWKS ✅

RBAC Middleware (on the API path) then:
	•	Parses JWT → org_id, roles/scopes
	•	Enforces allowlists (wallet/contract/function)
	•	Proceeds with business flow

Bank → Gateway (/token): mTLS + API key
	2.	Gateway → RBAC (/token): pass through
	3.	RBAC → Keycloak: client_credentials with bankX-api-client
	4.	Keycloak → RBAC: returns JWT (aud=my-enterprise-api, org=bankX)
	5.	RBAC → Bank: returns JWT
	6.	Bank → Gateway (/bridge/deposit): mTLS + API key + Bearer JWT
	7.	Gateway: validates JWT (JWKS) + API key; forwards
	8.	RBAC: enforces RBAC/allowlists; executes


Pattern A (preferred — what you’re already designing)
	•	Partner → RBAC: mTLS + API key (no Keycloak secret).
	•	RBAC → Keycloak: uses stored client_id/secret per bank (in Secrets Manager/Vault).
	•	RBAC /token returns Keycloak access_token (JWT) to the bank.
	•	Bank uses that JWT + mTLS + API key for API calls.



====
Got it—here’s the exact flow you described, clean and precise.

Flow A — RBAC issues JWT on behalf of the bank

Preconditions / Mappings
	•	api_key_id ↔ tenant_id ↔ oidc_client_id ↔ secret_ref (in DB)
	•	secret_ref points to Secret Manager entry for the OIDC client secret
	•	Cert fingerprint for the bank is registered (for mTLS)

Sequence (Mermaid)

sequenceDiagram
    participant Bank as Bank Backend
    participant GW as API Gateway
    participant RBAC as RBAC Middleware
    participant SM as Secret Manager
    participant IdP as IdP (OIDC)

    Note over Bank: 1) Get JWT via RBAC
    Bank->>GW: POST /auth/jwt (mTLS) <br/>Headers: x-api-key
    GW->>GW: Verify mTLS client cert ✅<br/>Verify API key ✅ (get api_key_id)
    GW->>RBAC: Forward request <br/>Headers: X-Api-Key-Id, X-Client-Cert-Fingerprint

    RBAC->>RBAC: Lookup api_key_id → (tenant, oidc_client_id, secret_ref)
    RBAC->>SM: Get client_secret (secret_ref)
    SM-->>RBAC: client_secret
    RBAC->>IdP: POST /oauth2/token (client_credentials)<br/>client_id=oidc_client_id&client_secret
    IdP-->>RBAC: { access_token (JWT), expires_in }
    RBAC-->>GW: { access_token (JWT) }
    GW-->>Bank: { access_token (JWT) }

    Note over Bank: 2) Call protected API
    Bank->>GW: POST /settlement/v1/... (mTLS)<br/>Headers: x-api-key, Authorization: Bearer JWT
    GW->>GW: Verify mTLS ✅, verify API key ✅, rate-limit/log
    GW->>RBAC: Forward request + JWT<br/>Headers: X-Api-Key-Id, X-Client-Cert-Fingerprint
    RBAC->>RBAC: Verify JWT (JWKS cache), resolve tenant, enforce RBAC/allowlists
    RBAC-->>GW: 200/401/403/429/5xx
    GW-->>Bank: Response

Endpoints & Headers

1) Get JWT (Bank → RBAC via Gateway)

POST /auth/jwt
x-api-key: <BANK_API_KEY>
# mTLS client cert presented at TLS layer
Body: { "scope": "settlement.write" }   # optional

Gateway → RBAC (forwarded):

X-Api-Key-Id: <api_key_id>
X-Client-Cert-Fingerprint: sha256:<...>
X-Request-Id: <uuid>

RBAC → IdP (client credentials):

POST /oauth2/token
Content-Type: application/x-www-form-urlencoded

grant_type=client_credentials&
client_id=<oidc_client_id>&
client_secret=<resolved_from_secret_manager>&
scope=settlement.write

RBAC → Bank (via Gateway):

{
  "access_token": "<JWT>",
  "token_type": "Bearer",
  "expires_in": 600
}

2) Protected API call

POST /settlement/v1/submit
Authorization: Bearer <JWT>
x-api-key: <BANK_API_KEY>
# mTLS client cert at TLS

What each component does
	•	API Gateway
	•	Verifies mTLS and API key; resolves/attaches X-Api-Key-Id
	•	Rate limits, quotas, logging; forwards fingerprint header
	•	RBAC Middleware
	•	For /auth/jwt: DB lookup → fetch client_secret from Secret Manager → OIDC token request → return JWT
	•	For protected APIs: verify JWT (JWKS cache), map tenant, enforce RBAC + allowlists (wallets/contracts/methods), audit
	•	Secret Manager
	•	Stores OIDC client_secret; returns on-demand (never logs it)
	•	IdP (OIDC)
	•	Issues short-lived access tokens for client_credentials

Security & Ops tips (for this model)
	•	Do not expose or log client_secret; only RBAC reads from Secret Manager.
	•	Cache the JWT per tenant until expiry; proactively refresh on 80–90% TTL.
	•	Enable CRL/OCSP on Gateway for client-cert revocation.
	•	Use mTLS-bound tokens (RFC 8705) if your IdP supports it (extra strong binding).
	•	Rotate: API keys, client secrets; monitor token issuance rates (abuse detection).
	•	In RBAC, verify JWT claims: iss, aud, exp, nbf, scope/roles, sub.

If you want, I can tailor the headers and config for your stack (e.g., Kong, AWS API Gateway, or NGINX + Keycloak/Okta), including example plugin settings and JWKS caching code.

Onboarding (new bank)
	1.	PKI / mTLS

	•	Platform CA issues client certificate for the bank (or signs bank CSR).
	•	Distribute: bank-client.crt + bank-client.key (to bank) and Root/Intermediate CA (to both sides).
	•	Register the bank cert thumbprint (SHA-256) in your tenant table.

	2.	API Key (Gateway)

	•	Create API key in the API Gateway; attach usage plan (rate/quotas) and tenant tag.
	•	Store mapping in DB: bank_id ↔ api_key_id ↔ cert_thumbprint.

	3.	OIDC Client (IdP)

	•	Create OIDC client (grant: client_credentials for system-to-system).
	•	Get client_id/client_secret; assign scopes/roles.
	•	Store mapping in DB: bank_id ↔ oidc_client_id ↔ scopes/roles.
	•	(Optional, strong) Enable MTLS-bound tokens at IdP so the JWT is tied to the bank cert.

API Gateway
The API Gateway acts as the secure entry point for all requests, enforcing mTLS for mutual authentication, validating API keys, and applying rate limiting to protect against abuse. It provides identification and traffic control for each tenant, while also enabling simple service integration by routing validated requests to the RBAC Middleware and downstream services.

Identity Provider (IdP)
The Identity Provider (IdP) implements OpenID Connect (OIDC) on top of the OAuth2 protocol, enabling strong authentication and standardized token issuance. It manages roles, claims, and allowlists, providing signed JWTs that the RBAC Middleware can verify to enforce fine-grained authorization across APIs and smart contract operations.

Prototype Phase
In the prototype phase, RBAC policies are mocked within the middleware to validate functional flows without requiring a full identity infrastructure. The middleware is also responsible for sending blockchain transactions (received from the Mock Backoffice Application) to the Validium RPC Node and for covering the gas fees. This setup enables rapid iteration, testing, and integration of the solution.

Production Phase
In the production phase, the RBAC middleware should be integrated with an Identity Provider (IdP) to enable fine-grained authorization. OpenConnect ID should be configured so that JWT tokens issued by the IdP are validated against RBAC policies before any transaction is processed. Responsibility for signing transactions and paying gas fees should be shifted to the RelayerSigner component, which will handle these functions.

mTLS + PKI – The CA issues a client certificate and sends it to the bank.
	2.	API Key – An API key is generated in the API Gateway and provided to the bank.
	3.	OIDC Client – The IdP creates a client_id and client_secret, mapped in the DB with the api_key_id.
	4.	JWT Retrieval – The bank calls RBAC Middleware with API key + client certificate → Gateway verifies them → RBAC fetches secret from Secret Manager and obtains a JWT from the IdP.
	5.	Protected API Access – The bank calls APIs using API key + client certificate + JWT.

RBAC ensures that the off-chain state is accessible only to authorized parties and should be enforced at the API layer, which sits between the application services and the Validium Swift node RPC.

When external users connect via the API, they should be identified using the API key provided by the API Gateway. Communication is secured through mTLS/PKI, where the API Gateway verifies the client certificate. The Identity Provider (IdP), configured with OpenID Connect (OIDC), provides fine-grained identity and authorization by issuing JWT tokens that are validated against RBAC policies.

sequenceDiagram
    participant CA as Certificate Authority (CA)
    participant GW as API Gateway
    participant IdP as IdP / OIDC
    participant DB as Database
    participant RBAC as RBAC Middleware
    participant Bank as Bank

    Note over Bank,CA: Step 1 - mTLS + PKI
    CA->>Bank: Issue client certificate (X.509)
    Bank-->>CA: Stores cert & private key

    Note over Bank,GW: Step 2 - API Key
    GW->>Bank: Generate & provide API key
    GW->>DB: Store api_key_id mapping

    Note over Bank,IdP: Step 3 - OIDC Client
    IdP->>RBAC: Create client_id + secret
    RBAC->>DB: Map (api_key_id, client_id, client_secret_ref)
    IdP-->>Bank: Provide client_id + secret


