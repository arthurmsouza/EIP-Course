Identity & Access Management (IAM) Requirements

Infrastructure Configuration
	•	✅ Setup Keycloak service as the central IdP managing AuthN (authentication) and AuthZ (authorization) for Paladin and Validium participants.
	•	✅ Expose Keycloak BackOffice endpoint over TLS and share admin credentials securely.
	•	✅ Setup SMTP server for 2FA (email verification) in authentication flow.

⸻

🛠 Keycloak BackOffice Configuration
	•	✅ A single realm per environment (e.g., test, prod) hosts all organizations.
	•	✅ Each organization is represented by a group or attribute (org_id, bic, tenant_id) within the realm.
	•	✅ Clients (applications) are registered in Keycloak and authenticate using client_id and client_secret via OpenID Connect (OIDC) protocol.
🟢 (Your original line was fine; I just corrected “clients d” → “client_id”).
	•	✅ Authentication handled via OIDC protocol (no PKCE).
	•	✅ Single Sign-On (SSO) enabled for Paladin and Validium applications through Keycloak.
	•	✅ Keycloak validates user credentials and issues:
	•	ID Token containing identity claims (username, roles, org, bic, etc.)
	•	Access Token used to request access to protected APIs
	•	✅ Tokens are JWT-encoded and signed; validated by backend services using Keycloak’s JWKS endpoint.
	•	✅ Two-Factor Authentication (2FA) integrated into the OIDC login flow through Keycloak and enforced per user or per realm.
	•	✅ 2FA supports email verification (requires SMTP server configuration).
	•	✅ Mapped Role-Based Access Control (RBAC) via Keycloak realm roles:
	•	admin, auditor, and operator.

⸻

✅ Summary

✅ Your list covers all essential Keycloak minimum requirements for:
	•	Centralized IdP setup
	•	OIDC client registration (client_id/client_secret)
	•	JWT issuance and validation
	•	SSO and MFA
	•	Role-based access control
	•	Secure endpoint and SMTP setup